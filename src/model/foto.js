export class Foto{
    constructor (id, filename, titulo, descripcion, fecha){
        this.id = id,
        this.filename = filename,
        this.titulo = titulo,
        this.descripcion = descripcion,
        this.fecha = fecha
    }
}